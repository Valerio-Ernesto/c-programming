#include<stdio.h>

int main(void)
{
	int first, second, third;
	float randomOne, arbitrary, variable;

	printf("Estas variables no fueron inicializadas.\n");
	printf("Números enteros: %d, %d y %d.\n", first, second, third);
	printf("Números reales: %f, %f y %f.", randomOne, arbitrary, variable);

	return 0;
}
