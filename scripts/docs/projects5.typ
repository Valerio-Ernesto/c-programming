= C Programming A Modern Approach

== Chapter 5 Selection Statements 
//@reference 

+ Write a program that calculates how many digits a number contains:

  ```
  Enter a number: 374
  The number 374 has 3 digits
  ```

  You may assume that the number has no more than four digits. _Hint:_ Use `if` statements to 
  test the number. For example, if the number is between 0 and 9, it has one digit. If the number
  is between 10 and 99, it has two digits.

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Write a program that asks the user for a 24-hour time, then displays the time in 12-hour form:

  ```
  Enter a 24-hour time: 21:11
  Equivalent 12-hour time: 9:11 PM
  ```
  
  Be careful not to display 12:00 as 0:00.

  #underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

#bibliography("references.yml")

