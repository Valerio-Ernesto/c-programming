= C Programming A Modern Approach

== Chapter 4 Expressions@reference 

+ Write a program that asks the user to enter a two-digit number, then prints the number with
  its digits reversed. A session with the program should have the following appearance:

  ```
  Enter a two-digit number: 28
  The reversal is: 82
  ```

  Read the number using `%d`, then break it into two digits. _Hints:_ It `n` is an integer, then `n % 10`
  is the last digit in `n` and `n / 10` is `n` with the last digit removed.

  #underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Extend the program in Programming Project 1 to handle _three_-digit numbers.

  #underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

+ Mini project instructions.

```
Code example
```

#underline("Answer"): #text(fill:olive)[#link("files/chapter/src/")[Source Code]]

#bibliography("references.yml")
